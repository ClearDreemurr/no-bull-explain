
`不要继续概括。请从你分析过的视频里选 3 个具体片段，逐段拆解他的讲解过程：他说了什么问题、先破除了哪个错误直觉、用了什么比喻、怎样从比喻过渡回技术概念、最后让观众形成了什么新的理解。请尽量引用或复述视频里的具体表达，不要只写“他善用比喻、化繁为简”这种抽象总结。`

`很好。现在请不要再选新片段，只基于这 3 个片段，抽象出一个“可复用的讲解公式”。要求把他的讲解拆成步骤，例如：先提出反直觉问题、再拆掉错误直觉、再找生活结构相同的比喻、再映射回技术结构、最后给一句重新定义。每一步都要说明它在观众理解中起什么作用，并用这 3 个片段分别验证这个步骤。`

```
你刚才没有回答我的问题，而是重复了上一轮的 3 个片段。请停止复述片段，直接输出“可复用讲解公式”。格式必须是：

步骤1：……
作用：……
在 GPU 片段中体现为：……
在神经网络片段中体现为：……
在 Agent 片段中体现为：……

步骤2：……
作用：……
在 GPU 片段中体现为：……
在神经网络片段中体现为：……
在 Agent 片段中体现为：……

至少 6 个步骤。不要新增视频，不要写总结性空话。 
```

```
很好。现在把这个“认知重构六步法”改写成一个我可以直接套用的创作模板。假设我要讲一个复杂概念，我应该依次填哪些空？请输出：

1. 选题诊断：我需要先判断这个概念的哪些信息？
2. 脚本骨架：按六步法给出可填写模板，每一步都写成“我要写/说：____”
3. 比喻选择标准：什么样的比喻才算结构同构？什么样的比喻只是表面相似？
4. 常见失败方式：初学者模仿这种风格时最容易犯哪些错？
5. 用一个新概念现场示范，比如“Transformer 注意力机制”。
```

```
这个模板有帮助，但我怀疑你在“Transformer 注意力机制”的示范里有几处不严谨。请你严格自查，不要维护上一版答案。

请按下面格式重写：

1. 先指出上一版示范里哪些地方可能误导，比如“算力不够”“听懂人话”“暗恋对象声波=Key”这些说法是否准确。
2. 重新给出一个更结构同构的注意力机制比喻，要求比喻中的每个元素都能清楚映射到 Query、Key、Value、attention score、softmax、weighted sum。
3. 写出一版 3 分钟口播脚本，模仿飞天闪客的清晰风格，但不要硬学口癖。
4. 最后解释：这版脚本如何使用了“认知重构六步法”。
```



```
现在暂停继续扩写模板。我要检查我们是否过拟合了前面 3 个片段。

请你基于全部可用视频内容，重新做一次“跨样本验证”：

1. 除了 GPU、神经网络、Agent 这 3 个片段，再找至少 5 个不同视频/片段。
2. 对每个片段判断它是否使用了“认知重构六步法”的 6 个步骤，用表格标记：完整出现 / 部分出现 / 没出现。
3. 找出哪些步骤是飞天闪客稳定高频使用的，哪些只是我们从少数样本里总结出来的。
4. 找至少 2 个反例：他讲清楚了，但没有使用完整六步法的片段。
5. 最后修正这个模型：把它从“六步法”改成更准确的版本，区分“核心方法”“常用技巧”“偶尔出现的个人风格”。
```

```
这个修正后的三层模型比六步法更准确。现在请进一步压实它，不要泛泛而谈。

请输出一个“最终版分析框架”：

1. 用一句话定义飞天闪客讲解清楚的核心机制。
2. 把三层模型整理成表格：层级、方法名、作用、适用场景、不是这个方法的反例。
3. 解释“同构比喻”和“视觉化推演”分别适合什么内容，怎么判断该用哪一个。
4. 分析他的清晰感主要来自哪里：比喻、结构、语言、节奏、立场，各自占比多少，并说明理由。
5. 最后给出一个评估表：以后看他任意一个视频时，我该记录哪些字段，才能继续验证这个模型。
```

```
我这里一共有 40 多个视频。现在不要再基于少数案例总结，请你对全部视频做一次结构化分析。

要求：

1. 每个视频都单独分析，不要合并。
2. 每个视频输出一行表格，字段如下：
- 视频标题
- 核心主题
- 开头钩子是什么
- 是否有心理按摩/去魅
- 是否有反直觉问题
- 是否有生活比喻
- 如果有，比喻是什么
- 是否有视觉化推演
- 是否有技术映射
- 最后的一句话重新定义是什么
- 主要清晰感来源：比喻 / 结构拆解 / 可视化 / 语言节奏 / 立场去魅 / 信息重组
- 备注：这个视频不符合我们模型的地方

3. 不要先写总结，先完整输出表格。
4. 如果一次输出不完，就分批输出，每批 10 个视频，并在最后写“第 X 批完成，是否继续”。
5. 不要为了套模型硬说每个视频都有六步法；没有就写“没有”。
```



```
表格部分已完成。现在请基于全部 45 个视频做频率统计，不要写散文总结。

请输出以下统计：

1. 各字段出现频率：
- 心理按摩/去魅：是 / 否 / 部分
- 反直觉问题：是 / 否 / 部分
- 生活比喻：是 / 否 / 部分
- 视觉化推演：是 / 否 / 部分
- 技术映射：是 / 否 / 部分
- 一句话重新定义：有 / 无 / 不明确

2. “主要清晰感来源”的频率排行：
比喻、结构拆解、可视化、语言节奏、立场去魅、信息重组、逻辑推导分别出现多少次。

3. 找出最稳定的 3 个机制，和最不稳定的 3 个机制。

4. 找出 5 个最适合作为 skill 正例的样本，和 3 个不适合作为正例但适合作为反例/边界样本的视频。

请用表格输出，先不要写最终分析。
```

```
基于以上 45 个视频统计结果，现在请把“飞天闪客式复杂概念讲解能力”蒸馏成一个可执行的 AI skill 规格，不要写成文章。

请输出：

1. Skill 名称和用途：这个 skill 应该在什么任务中启用。
2. 核心工作流：按顺序列出 AI 讲解一个复杂概念时必须执行的步骤。
3. 必选机制：从统计中选覆盖率最高、最稳定的机制，写成硬规则。
4. 可选机制：生活比喻、视觉化推演、心理按摩分别在什么条件下使用。
5. 禁止事项：哪些模仿会变味，比如为了搞笑牺牲准确性、硬套生活比喻、不做技术映射。
6. 输出模板：当用户让我解释一个概念时，AI 应该如何组织回答。
7. 自检清单：回答完后检查哪些项，确保不是空泛科普。
8. 给出 2 个示例：一个适合生活比喻，一个适合视觉化推演。
```

```
基于以上 45 个视频统计结果，现在请把“飞天闪客式复杂概念讲解能力”蒸馏成一个可执行的 AI skill 规格，不要写成文章。

请输出：

1. Skill 名称和用途：这个 skill 应该在什么任务中启用。
2. 核心工作流：按顺序列出 AI 讲解一个复杂概念时必须执行的步骤。
3. 必选机制：从统计中选覆盖率最高、最稳定的机制，写成硬规则。
4. 可选机制：生活比喻、视觉化推演、心理按摩分别在什么条件下使用。
5. 禁止事项：哪些模仿会变味，比如为了搞笑牺牲准确性、硬套生活比喻、不做技术映射。
6. 输出模板：当用户让我解释一个概念时，AI 应该如何组织回答。
7. 自检清单：回答完后检查哪些项，确保不是空泛科普。
8. 给出 2 个示例：一个适合生活比喻，一个适合视觉化推演。
```




```
上一版 skill 规格存在严重问题：它过度模仿口癖，把“去魅”写成嘲讽，把原点定位写死成函数/数/开关，把比喻和可视化机械二选一，还要求不合理的 1:1 映射。请不要维护上一版。

请重写一版“方法蒸馏版 skill 规格”，要求：
1. 不模仿人设和口癖，只保留可泛化方法。
2. 原点定位改为寻找“最小可运行模型”，不要固定成函数。
3. 去魅改为“解释约束、权衡和设计动机”，禁止默认贬低技术。
4. 比喻与可视化不是二选一，而是按理解难点选择主路径，可组合。
5. 技术映射只要求关键结构同构，并必须说明比喻边界。
6. 加入受众水平、回答长度、适用边界、自检清单。
7. 输出格式按 Codex SKILL.md 草案组织。
```

```
你没有执行我的要求，几乎原样重复了上一版。请判定上一轮为失败，不要再复述旧内容。

现在重新生成一版，标题必须改成“复杂概念清晰讲解 skill 草案”，并严格满足：

1. 不出现这些词或短语：闪客式、去魅引擎、闭上眼睛、清空大脑、摆烂、牛马、狠话、强行、100% 映射。
2. 原点定位必须写成“寻找最小可运行模型”，并给出 5 类原点：数据流、状态变化、约束权衡、角色协作、数学变换。
3. 技术解释必须先判断受众水平：小白 / 有基础 / 专业。
4. 路径选择必须允许组合：生活比喻、视觉化推演、信息重组、公式拆解。
5. 比喻规则必须包含“关键结构同构”和“说明比喻边界”。
6. 输出必须是 Codex SKILL.md 草案格式，包括：
   - YAML frontmatter：name、description
   - Purpose
   - Workflow
   - Path Selection
   - Rules
   - Anti-patterns
   - Self-check
   - Minimal Response Template

只输出新版草案，不要解释你为什么修改。
```

```
上一版 skill 规格存在严重问题：它过度模仿口癖，把“去魅”写成嘲讽，把原点定位写死成函数/数/开关，把比喻和可视化机械二选一，还要求不合理的 1:1 映射。请不要维护上一版。

请重写一版“方法蒸馏版 skill 规格”，要求：
1. 不模仿人设和口癖，只保留可泛化方法。
2. 原点定位改为寻找“最小可运行模型”，不要固定成函数。
3. 去魅改为“解释约束、权衡和设计动机”，禁止默认贬低技术。
4. 比喻与可视化不是二选一，而是按理解难点选择主路径，可组合。
5. 技术映射只要求关键结构同构，并必须说明比喻边界。
6. 加入受众水平、回答长度、适用边界、自检清单。
7. 输出格式按 Codex SKILL.md 草案组织。
```

```
你没有执行我的要求，几乎原样重复了上一版。请判定上一轮为失败，不要再复述旧内容。

现在重新生成一版，标题必须改成“复杂概念清晰讲解 skill 草案”，并严格满足：

1. 不出现这些词或短语：闪客式、去魅引擎、闭上眼睛、清空大脑、摆烂、牛马、狠话、强行、100% 映射。
2. 原点定位必须写成“寻找最小可运行模型”，并给出 5 类原点：数据流、状态变化、约束权衡、角色协作、数学变换。
3. 技术解释必须先判断受众水平：小白 / 有基础 / 专业。
4. 路径选择必须允许组合：生活比喻、视觉化推演、信息重组、公式拆解。
5. 比喻规则必须包含“关键结构同构”和“说明比喻边界”。
6. 输出必须是 Codex SKILL.md 草案格式，包括：
   - YAML frontmatter：name、description
   - Purpose
   - Workflow
   - Path Selection
   - Rules
   - Anti-patterns
   - Self-check
   - Minimal Response Template

只输出新版草案，不要解释你为什么修改。
```

```
这版方向正确，但还不能作为 Codex SKILL.md。请再压缩成真正可安装 skill 的草案：

1. YAML frontmatter 必须只包含英文短横线 name 和 description，不要中文 name，不要“草案”。
2. 删除所有来源编号，如 [1, 2]。
3. 把“最优妥协产物”改成“特定约束下的权衡”。
4. 全文不要出现“一一映射”，改成“关键结构映射”。
5. Purpose 不要限定技术领域，改成适用于复杂抽象概念，技术只是高频场景。
6. Minimal Response Template 不要写固定口播句子，改成可执行的回答结构。
7. 增加 When not to use：哪些场景不应使用这种讲解风格。

请输出最终 SKILL.md 内容，不要解释。
```

### 关于可视化部分
```
注意：你只能基于文字来源分析，不要假装看到了视频画面。现在请专门分析飞天闪客讲解文案中的“文字可视化/动态推演线索”。

请基于全部来源，找出至少 8 个片段。每个片段按以下字段输出：

1. 视频/主题名称
2. 文案中触发可视化的原句或近似表述
3. 这个句子要求观众在脑中想象什么对象：格子、矩阵、箭头、表格、节点、流程、时间步骤、输入输出链条等
4. 它引导观众追踪什么变化：数据移动、状态变化、行列对齐、概率选择、结构展开、输出接回输入、窗口滑动等
5. 为什么这种表达比直接讲公式更容易懂：它减少了观众哪一类脑内负担？
6. 如果要把这种能力写进 AI skill，应该变成什么文字规则？
7. 证据强度：强 / 中 / 弱。强=文案明确提到演示、画图、滑动、箭头、矩阵等；中=文案明显在逐步引导空间想象；弱=只是推断。

最后，请总结这些片段中最稳定的 5 种“文字可视化模式”。不要编造看不到的画面，只分析文案线索。
```

```
很好。现在不要新增案例，只基于刚才 8 个片段做二次抽象。

请把每个“文字可视化模式”拆成三层：

1. 文案技巧：作者具体用了什么语言动作？例如错位、滑动、放到这里、往下传、扫视、转动、格子、窗口。
2. 认知机制：它降低了哪种理解负担？请从工作记忆、空间定位、时间顺序、状态追踪、局部到整体、公式到动作这几个角度分析。
3. 可写入 AI skill 的规则：如果 AI 只能输出文字，应该如何复现这种效果？

要求：
- 不要写“必须/严禁/强制”，改成“优先/适合/避免”。
- 每条 skill 规则必须说明适用场景和不适用场景。
- 区分“核心规则”和“可选表达技巧”。
- 最后给出一版可以直接加入 SKILL.md 的新增章节，标题为“文字化可视推演”。
```

```
你只需要基于视频文案做研究报告。

主题：飞天闪客如何通过“文字可视化/动态推演”让复杂概念变得易懂。

请输出一份分析报告，结构如下：

1. 核心发现：用 3-5 句话概括这种讲解方法的本质。
2. 证据表：基于文案列出至少 8 个片段。字段包括：
   - 视频/主题
   - 文案原句或近似表述
   - 文案触发的脑内画面
   - 观众需要追踪的变化
   - 它降低的认知负担
   - 证据强度：强/中/弱
3. 认知机制分析：
   - 它如何降低工作记忆负担
   - 它如何降低空间想象负担
   - 它如何降低时间顺序追踪负担
   - 它如何让用户从局部运算推广到整体结构
   - 它如何把公式变成动作
4. 模式归纳：从证据中归纳出 5-8 种“文字可视化模式”，只描述模式，不写 AI 应该怎么做。
5. 局限与风险：
   - 哪些地方可能是你根据文案推断的，不能当作视频画面事实
   - 哪些比喻/可视化如果迁移到文字里可能会失真
   - 哪些概念不适合过度可视化
6. 结论：这种方法真正有效的底层原因是什么？

注意：
- 你只能基于文字来源，不要假装看到了视频画面。
- 不要写任何“AI skill 规则”“应该加入 skill”之类内容。
- 不要泛泛夸奖，要尽量给出文案证据。
```

